<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sparkit.system.mapper.MenuMapper">

    <select id="selectMenusByUserId" resultType="com.sparkit.system.model.entity.Menu">
        SELECT DISTINCT m.*
        FROM sparkit_menu m
        INNER JOIN sparkit_role_menu rm ON m.id = rm.menu_id
        INNER JOIN sparkit_admin_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND m.status = 1
          AND m.deleted = 0
        ORDER BY m.sort ASC
    </select>

</mapper>